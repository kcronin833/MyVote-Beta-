Drop the merch mockup sheet here as: lookbook.jpg

The /store page renders public/merch/lookbook.jpg as the lookbook hero.
Until the file exists, the lookbook hides itself automatically.

To publish it:
1. Save the "Undefined/Undecided 2028 · CITIZN.APP" mockup sheet as
   public/merch/lookbook.jpg (a .jpg or .png ~1200px wide is ideal).
2. Commit + push (Vercel serves it as a static asset at /merch/lookbook.jpg).
